{
  "name": "leadops",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "node -e \"try{require('child_process').execSync('python -c \\\"import playwright\\\"',{stdio:'pipe'})}catch(e){console.error('ERROR: playwright not installed. Run: npm run setup');process.exit(1)}\" && python -m uvicorn backend.core.server:app --reload --host 127.0.0.1 --port 8000",
    "dev": "concurrently -n fe,be -c cyan,yellow \"npm run dev:frontend\" \"npm run dev:backend\"",
    "build": "cd frontend && npm run build",
    "lint:py": "ruff check .",
    "lint:ts": "cd frontend && npx tsc --noEmit",
    "lint": "npm run lint:py && npm run lint:ts",
    "test:py": "pytest",
    "test:ts": "cd frontend && npx vitest run",
    "test": "npm run test:py && npm run test:ts",
    "clean": "node -e \"require('fs').rmSync('frontend/dist',{recursive:true,force:true}); require('fs').rmSync('storage/screenshots',{recursive:true,force:true}); ['csv','json','parquet','xlsx'].forEach(e=>require('fs').readdirSync('content').forEach(d=>require('fs').readdirSync('content/'+d).filter(f=>f.endsWith('.'+e)).forEach(f=>require('fs').rmSync('content/'+d+'/'+f))))\"",
    "setup": "python -m venv venv && pip install -r requirements.txt && playwright install chromium && cd frontend && npm install",
    "check:playwright": "python -c \"import playwright; print('ok')\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
